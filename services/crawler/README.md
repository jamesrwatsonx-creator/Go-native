# Crawler service

Ingests only registry-approved public sources through provider adapters. It records provenance, honors policy/terms, queues review, and never publishes raw extraction directly to users.
