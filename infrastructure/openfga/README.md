# OpenFGA authorization model

Use relationship tuples to represent workspace membership, case collaboration, document sharing, and verification responsibilities. Add the generated model and migrations only after a domain permission review; no permissive wildcard relations.
