# OpenBao secrets boundary

Production secrets are never committed. OpenBao will issue/rotate scoped credentials for databases and connector providers. Local development uses a documented, non-production secret bootstrap later.
